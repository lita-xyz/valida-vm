[package]
name = "valida-elliptic"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
p3-field = { workspace = true }
valida-machine = { path = "../machine" }
valida-cpu = { path = "../cpu" }
valida-opcodes = { path = "../opcodes" }
halo2curves = "0.8.0"
